<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Teams_Widget.MaterialComponents.Chip.Choice" parent="Widget.MaterialComponents.Chip.Choice">
        <!-- Chip background color selector -->
        <item name="chipBackgroundColor">@color/teams_chip_color</item>
        <!-- Border color -->
        <item name="chipStrokeColor">@color/chip_teams_stroke_color</item>

        <!-- Chip text color selector -->
        <item name="android:textColor">@color/teams_chip_text_color</item>
        <!-- Chip close icon color selector -->
        <item name="closeIconTint">@color/mtrl_chip_close_icon_tint</item>
    </style>

</resources>